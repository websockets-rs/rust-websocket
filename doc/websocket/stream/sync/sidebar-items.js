initSidebarItems({"enum":[["Shutdown","Possible values which can be passed to the [`shutdown`] method of [`TcpStream`]."]],"struct":[["TcpStream","A TCP stream between a local and a remote socket."],["TlsStream","A stream managing a TLS session."]],"trait":[["AsTcpStream","The ability access a borrow to an underlying TcpStream, so one can set options on the stream such as `nonblocking`."],["NetworkStream","a `Stream` that can also be used as a borrow to a `TcpStream` this is useful when you want to set `TcpStream` options on a `Stream` like `nonblocking`."],["Splittable","some streams can be split up into separate reading and writing components `TcpStream` is an example. This trait marks this ability so one can split up the client into two parts."]]});